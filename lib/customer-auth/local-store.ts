/**
 * Deprecated: customer data is now persisted in the database via Prisma and
 * surfaced through server actions in `actions/customer-auth.ts`. This file is
 * intentionally a no-op stub to avoid breaking any stray imports that have not
 * been migrated yet; new code should not depend on it.
 */
export {};
